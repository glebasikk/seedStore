const joi = require('joi');





class Seed {
        async allSortedSeeds(req,res) {
                const queryValidation = joi.object({
                        page: joi.number().integer().min(1)    
                });
                cons
        }
    }
    

    
    module.exports = new Seed();